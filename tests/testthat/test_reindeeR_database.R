# Legacy database import tests
# import_recordings() has been removed from the package.

require(testthat)

test_that("Importation of speech signal files works", {
  skip("import_recordings() removed — functionality deprecated")
})

test_that("Importation of EGG and speech signal files works", {
  skip("import_recordings() removed — functionality deprecated")
})
