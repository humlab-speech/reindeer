% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_validation.R
\name{validate_corpus}
\alias{validate_corpus}
\title{Validate a corpus's JSON files against reindeer schemas}
\usage{
validate_corpus(corpus_obj)
}
\arguments{
\item{corpus_obj}{A \code{corpus} object.}
}
\value{
A tibble with columns \code{file}, \code{schema}, \code{level}, \code{ok}.
}
\description{
Walks the corpus and reports schema-validation issues for the database
\verb{_DBconfig.json} and every \code{METADATA.json} under it. Returns a tibble
summarising files validated and any issues found.
}
\details{
Validation is soft-warn by default (existing non-conformant files emit a
\code{cli} warning rather than aborting). Set
\code{options(reindeer.schema_strict = TRUE)} to make any issue an error.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
corp <- corpus("path/to/db_emuDB")
validate_corpus(corp)
\dontshow{\}) # examplesIf}
}
