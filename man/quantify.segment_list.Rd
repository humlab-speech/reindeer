% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reindeer_segment_list.R
\name{quantify.segment_list}
\alias{quantify.segment_list}
\title{Quantify method for segment_list - Apply DSP to query results}
\arguments{
\item{object}{A segment_list object (from ask_for/query)}

\item{dsp_function}{A DSP function from superassp or similar}

\item{...}{Additional arguments passed to the DSP function}

\item{.at}{Optional vector of relative time points (0-1) to extract from track}

\item{.use_metadata}{Logical; whether to use bundle metadata for parameter derivation}

\item{.verbose}{Logical; show progress messages}

\item{.parallel}{Logical; use parallel processing (default TRUE)}

\item{.workers}{Number of parallel workers (default: parallel::detectCores() - 1)}

\item{.use_cache}{Logical; enable result caching (default FALSE)}

\item{.cache_dir}{Character; cache directory path (default: tempdir()/reindeer_cache)}

\item{.cache_format}{Character; serialization format - "auto" (uses qs if available,
otherwise base serialize), "qs" (faster, smaller, requires qs package),
or "rds" (base R serialize, slower, larger). Default: "auto"}

\item{.optimize}{Logical; use optimized processing (default TRUE)}
}
\value{
An extended_segment_list with segment information and DSP-derived measurements
}
\description{
Applies a DSP function to all segments in a segment_list, extracting
acoustic measurements for each segment. This is equivalent to emuR::get_trackdata()
but allows for custom DSP functions with metadata-driven parameters.
}
\examples{
\dontrun{
segs <- ask_for(corpus, "Phonetic == t")
formants <- quantify(segs, superassp::forest)

# Extract at specific time points
formants_at_midpoint <- quantify(segs, superassp::forest, .at = 0.5)
formants_three_points <- quantify(segs, superassp::forest, .at = c(0.2, 0.5, 0.8))

# Disable parallel processing
formants <- quantify(segs, superassp::forest, .parallel = FALSE)

# Enable caching with qs format (faster, recommended)
formants <- quantify(segs, superassp::forest, .use_cache = TRUE)

# Force base R serialization
formants <- quantify(segs, superassp::forest, .use_cache = TRUE, .cache_format = "rds")
}

}
