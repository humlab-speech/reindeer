% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reindeer_enrich.R
\name{derive_dsp_parameters}
\alias{derive_dsp_parameters}
\title{Derive DSP parameters from bundle metadata}
\usage{
derive_dsp_parameters(dsp_fun, metadata, metadata_fields, user_params)
}
\arguments{
\item{dsp_fun}{A DSP function whose formal arguments are matched against
the resolved norms.}

\item{metadata}{Named list of bundle metadata (expects \code{Age}, \code{Gender}).}

\item{metadata_fields}{Character vector of extra metadata field names to
map straight onto matching formals of \code{dsp_fun}.}

\item{user_params}{Named list of user overrides (win over derived norms).}
}
\value{
A named list of DSP parameters to pass to \code{dsp_fun}.
}
\description{
Internal helper. End users should call \code{\link[=dsp_parameters]{dsp_parameters()}} instead;
kept exported for companion packages (\code{erodex}) that share the
age/gender derivation logic.
}
\details{
Age/Gender are resolved to literature-derived, LOESS-smoothed norms
from the internal \code{DSPP} table (via \code{dspp_metadataParameters_dt()}),
matched to the DSP function's formal arguments. This is the same
lookup \code{\link[=dsp_parameters]{dsp_parameters()}} previews, so the preview equals what is
actually applied.
}
\keyword{internal}
