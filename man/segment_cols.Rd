% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_list_tidyselect.R
\name{segment_cols}
\alias{segment_cols}
\title{Select required segment_list columns}
\usage{
segment_cols()
}
\value{
A character vector of column names present in the current
tidyselect context.
}
\description{
Tidyselect helper for the 16 columns that every \code{segment_list} must carry
(\code{labels}, \code{start}, \code{end}, \code{db_uuid}, \code{session}, \code{bundle}, item IDs, level,
attribute, sequence indices, type, sample bounds, sample rate). Use it
inside \code{dplyr::select()} or any other tidyselect-aware verb to keep or
drop the segment columns as a group.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs |> dplyr::select(segment_cols())          # keep only required cols
segs |> dplyr::select(-segment_cols())         # drop them
\dontshow{\}) # examplesIf}
}
