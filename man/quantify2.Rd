% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_trackdata.R
\name{quantify2}
\alias{quantify2}
\title{Apply DSP Function to Two Segment Lists}
\usage{
quantify2(
  .what1,
  .what2,
  .from = NULL,
  .using,
  ...,
  .by_bundle = FALSE,
  .quiet = FALSE,
  .parallel = FALSE
)
}
\arguments{
\item{.what1}{Either a segment_list object, an EQL query string, or a corpus object}

\item{.what2}{Either a segment_list object or an EQL query string}

\item{.from}{corpus object (required if .what1 or .what2 are EQL queries)}

\item{.using}{DSP function to apply (must accept two data frame arguments)}

\item{...}{Additional arguments passed to the DSP function}

\item{.by_bundle}{Logical; if TRUE, process by bundle instead of by session (default: FALSE)}

\item{.quiet}{Logical; suppress messages (default: FALSE)}

\item{.parallel}{Logical; use parallel processing (default: FALSE)}
}
\value{
A list of results from applying the DSP function
}
\description{
Apply a DSP function that requires two segment lists as input, processing
them by session (default) or by bundle. This function is useful for analyses
that compare two different segmentation levels or annotations.
}
\details{
The function processes segments by merging two segment lists on a per-session
(or per-bundle if \code{.by_bundle = TRUE}) basis, then applying the DSP function
to each group. The DSP function must accept at least two data frame arguments.

If EQL query strings are provided, they are evaluated against the corpus to
generate segment lists.
}
\examples{
\dontrun{
# Using segment lists
vowels <- ask_for(corpus, "Phonetic = V")
consonants <- ask_for(corpus, "Phonetic = C")
results <- quantify2(vowels, consonants, .using = my_comparison_func)

# Using EQL queries directly
results <- quantify2("Phonetic = V", "Phonetic = C", 
                     .from = corpus, .using = my_comparison_func)

# Process by bundle instead of by session
results <- quantify2(vowels, consonants, .using = my_func, .by_bundle = TRUE)
}

}
