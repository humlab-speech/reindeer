% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reindeer_cmdi.R
\name{validate_cmdi}
\alias{validate_cmdi}
\title{Validate a generated CMDI record}
\usage{
validate_cmdi(path, xsd = NULL, online = FALSE)
}
\arguments{
\item{path}{Path to a \verb{*_cmdi.xml} file.}

\item{xsd}{Optional path to a local XSD to validate against.}

\item{online}{If \code{TRUE}, may fetch the CMD envelope schema from CLARIN when
no local XSD is available. Default \code{FALSE} (offline-safe).}
}
\value{
Invisibly, a list with \code{structural} (logical), \code{problems}
(character), and \code{xsd} (logical, or \code{NA} when not attempted).
}
\description{
Two layers. \strong{Structural} checks always run offline: a CMD 1.2 envelope
root, \code{CMDVersion = "1.2"}, the required \code{Header} fields, a
\code{ResourceProxyList}, and an \code{xsi:schemaLocation} that binds a profile
schema. \strong{XSD} validation is attempted when a schema is available — a
local \code{xsd}, a bundled envelope schema under \verb{inst/cmdi/}, or (with
\code{online = TRUE}) the CMD envelope schema fetched from CLARIN. The envelope
schema validates the envelope/Header/Resources; validating the profile
\code{Components} subtree needs the profile XSD (Component-level conformance is
delivered in a later phase). Full CLARIN compliance additionally requires
Schematron assertions applied by the external Java CMDI Instance Validator,
which is out of scope for this in-package check.
}
\seealso{
\code{\link[=create_cmdi_metadata]{create_cmdi_metadata()}}, \code{\link[=describe_corpus]{describe_corpus()}}.
}
