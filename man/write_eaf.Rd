% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop_elan.R
\name{write_eaf}
\alias{write_eaf}
\title{Write a segment_list as an ELAN .eaf file}
\usage{
write_eaf(seg, path, levels = NULL, media_url = NULL, author = "reindeer")
}
\arguments{
\item{seg}{A \code{segment_list} or compatible tibble.}

\item{path}{Output file path.}

\item{levels}{Optional character vector of levels to include. Default:
all levels present in \code{seg}.}

\item{media_url}{Optional URL or relative path written into the EAF
media descriptor.}

\item{author}{Author string written into the EAF root element.}
}
\value{
The file path, invisibly.
}
\description{
Writes a minimal but valid EAF 2.8 document derived from a
\code{segment_list} (or compatible tibble). Each requested level becomes a
\verb{<TIER>}; all annotations are \code{ALIGNABLE_ANNOTATION} referring to
deterministically allocated \verb{<TIME_SLOT>} IDs.
}
\details{
The function does not currently emit \verb{<PARENT_REF>} linkage; that
requires resolving \code{DBconfig$linkDefinitions}. A warning is issued when
link definitions are present in the corpus but tiers are written flat;
use the \code{levels} argument to restrict output to a known sub-hierarchy.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs <- query(corp, "Phonetic == V") |> collect()
write_eaf(segs, "phonetic.eaf", media_url = "audio/clip.wav")
\dontshow{\}) # examplesIf}
}
