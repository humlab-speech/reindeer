% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_size_management.R
\name{clean_all_caches}
\alias{clean_all_caches}
\title{Clean all caches for a corpus}
\usage{
clean_all_caches(
  corpus_obj,
  simulation_store = NULL,
  days_old = 30,
  dry_run = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{corpus_obj}{A corpus object}

\item{simulation_store}{Optional simulation store directory}

\item{days_old}{Remove files older than this many days (default: 30)}

\item{dry_run}{If TRUE, only show what would be deleted}

\item{verbose}{Show messages}
}
\value{
Named list with number of files deleted from each cache
}
\description{
Clean all caches for a corpus
}
\examples{
\dontrun{
corp <- corpus("path/to/db_emuDB")

# Dry run - see what would be deleted
clean_all_caches(corp, dry_run = TRUE)

# Actually delete files older than 30 days
clean_all_caches(corp)

# Delete files older than 7 days
clean_all_caches(corp, days_old = 7)
}
}
