% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus_describe.R
\name{describe_corpus}
\alias{describe_corpus}
\title{Emit standards-compliant corpus documentation}
\usage{
describe_corpus(
  corpus_obj,
  output_dir = NULL,
  formats = c("readme", "cmdi", "datacite", "cff", "jsonld"),
  profile = "speech-corpus",
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{corpus_obj}{A reindeer \code{corpus} object.}

\item{output_dir}{Directory to write outputs into. Defaults to the
corpus base path.}

\item{formats}{Character vector of formats to emit. Any subset of
\code{c("readme", "cmdi", "session-cmdi", "datacite", "cff", "jsonld")}.
\code{"session-cmdi"} additionally writes a media-session CMDI record per
session and per bundle (into the session/bundle directories) and links
them to the corpus record as a collection hierarchy.}

\item{profile}{CMDI profile (\code{"speech-corpus"}, \code{"media-corpus"},
\code{"speech-corpus-dlu"}).}

\item{force}{Logical; overwrite existing files.}

\item{verbose}{Logical; print progress.}
}
\value{
A named character vector of paths written, invisibly.
}
\description{
Produces README, CMDI XML, DataCite 4.5 JSON, CITATION.cff, and
schema.org JSON-LD for a corpus in one call. All formats are derived
from a single shared summary built by \code{collect_corpus_summary()}.
CMDI delegates to \code{create_cmdi_metadata()}.
}
\details{
Existing files at the target paths are never overwritten unless
\code{force = TRUE}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
corp <- corpus("path/to/db_emuDB")
describe_corpus(corp)
describe_corpus(corp, formats = c("cff", "jsonld"), force = TRUE)
\dontshow{\}) # examplesIf}
}
