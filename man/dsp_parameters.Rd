% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsp_parameters_public.R
\name{dsp_parameters}
\alias{dsp_parameters}
\title{Inspect age/gender-aware DSP parameters}
\usage{
dsp_parameters(age = NULL, gender = NULL, corpus_obj = NULL)
}
\arguments{
\item{age}{Numeric age in years. Required unless \code{corpus_obj} is supplied.}

\item{gender}{Character: "Female", "Male", or "Unspecified" (case-
insensitive; "F"/"M" also accepted). Required unless \code{corpus_obj}
is supplied.}

\item{corpus_obj}{Optional corpus object. When given, the function
pulls Age/Gender for every bundle and returns one row per bundle.}
}
\value{
A tibble with one row per requested (age, gender) — or one
row per bundle when \code{corpus_obj} is supplied. Columns mirror the
internal DSPP table (windowSize, minF, maxF, nominalF1, nominalF2,
nominalF3, plus any others present).
}
\description{
Returns the age/gender-appropriate DSP parameters that
\code{\link[=enrich]{enrich()}} and \code{\link[=quantify]{quantify()}} would pass to a DSP routine when
\code{.use_metadata = TRUE}. Use this to preview what \code{windowSize},
\code{minF}, \code{nominalF1/F2/F3} etc. will be applied before you run
a quantification, or to compare two speakers side-by-side.
}
\details{
Pass \code{age} and \code{gender} directly to look up a single row, or pass a
\code{corpus_obj} to get one row per bundle (resolved through the usual
session/bundle inheritance chain).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Single speaker
dsp_parameters(age = 35, gender = "Female")
# Compare two
rbind(
  dsp_parameters(age = 8,  gender = "Male"),
  dsp_parameters(age = 45, gender = "Male")
)
# Whole corpus
dsp_parameters(corpus_obj = corp)
\dontshow{\}) # examplesIf}
}
