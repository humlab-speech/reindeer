% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop_textgrid.R
\name{write_textgrid}
\alias{write_textgrid}
\title{Write a segment_list (or compatible tibble) as a Praat TextGrid}
\usage{
write_textgrid(seg, path, levels = NULL, short = FALSE, encoding = "UTF-8")
}
\arguments{
\item{seg}{A \code{segment_list} or compatible tibble. Must contain
\code{start}, \code{end}, \code{labels}, \code{level}; optionally \code{type}.}

\item{path}{Output file path.}

\item{levels}{Optional character vector of levels to include. By
default, all levels present in \code{seg}.}

\item{short}{Logical. Write the compact short-text format instead of
the more readable long format.}

\item{encoding}{Either \code{"UTF-8"} (default) or \code{"UTF-16LE"}.}
}
\value{
The file path, invisibly.
}
\description{
Writes a \code{.TextGrid} file (long format by default; short with
\code{short = TRUE}). Each requested level becomes a separate
\code{IntervalTier} (or \code{TextTier} when the rows' \code{type} column is \code{EVENT}).
}
\details{
Times are written in seconds. If the input columns are in milliseconds
(as \code{segment_list} start/end are), they are divided by 1000.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs <- query(corp, "Phonetic == V") |> collect()
write_textgrid(segs, "phonetic.TextGrid")
\dontshow{\}) # examplesIf}
}
