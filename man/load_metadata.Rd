% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_unified.R
\name{load_metadata}
\alias{load_metadata}
\title{Load corpus metadata from a single entry point}
\usage{
load_metadata(
  corpus_obj,
  source = c("files", "excel"),
  path = NULL,
  verbose = TRUE,
  parallel = TRUE
)
}
\arguments{
\item{corpus_obj}{A corpus object.}

\item{source}{One of \code{"files"} or \code{"excel"}. Default \code{"files"}.}

\item{path}{Path to the Excel workbook (required when
\code{source = "excel"}).}

\item{verbose}{Logical; show progress (forwarded to the underlying
loader when supported). Default \code{TRUE}.}

\item{parallel}{Logical; parallel JSON scan (only applies to
\code{source = "files"}). Default \code{TRUE}.}
}
\value{
The corpus object, invisibly.
}
\description{
One verb to populate the metadata cache regardless of where the
metadata lives. \code{source = "files"} rescans every \code{METADATA.json}
under the corpus (the former \code{\link[=gather_metadata]{gather_metadata()}} path).
\code{source = "excel"} reads a workbook produced by \code{\link[=export_metadata]{export_metadata()}}
and writes it back through the same channel as
\code{\link[=import_metadata]{import_metadata()}}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
corp <- corpus("path/to/db_emuDB")
load_metadata(corp)                                # files (default)
load_metadata(corp, source = "excel",
              path = "corpus_metadata.xlsx")       # excel round-trip
\dontshow{\}) # examplesIf}
}
