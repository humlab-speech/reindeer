% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_list_provenance.R
\name{dropped_rows}
\alias{dropped_rows}
\title{Per-step row-loss summary for a segment_list pipeline}
\usage{
dropped_rows(seg)
}
\arguments{
\item{seg}{A \code{segment_list} (or \code{extended_segment_list} /
\code{lazy_segment_list}, which is collected first).}
}
\value{
A tibble with one row per step: \code{step}, \code{verb}, \code{call},
\code{rows_in}, \code{rows_out}, \code{rows_lost}, \code{pct_lost}.
}
\description{
Tidy companion to \code{\link[=provenance]{provenance()}} and \code{\link[=dropped]{dropped()}} for debugging
filter/scout/ascend chains. Returns a one-row-per-step tibble with
the verb that ran, how many rows entered and left, and how many were
dropped. Use this when \code{\link[=dropped]{dropped()}} tells you something was lost and
you want to know \emph{where} in the pipeline.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs <- query(corp, "Phonetic == t") |>
        dplyr::filter(label != "never_matches") |>
        scout(1)
dropped_rows(segs)
\dontshow{\}) # examplesIf}
}
