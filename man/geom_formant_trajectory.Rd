% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_geoms.R
\name{geom_formant_trajectory}
\alias{geom_formant_trajectory}
\title{ggplot2 layer: formant trajectory lines}
\usage{
geom_formant_trajectory(mapping = NULL, data = NULL, formants = 1:3, ...)
}
\arguments{
\item{mapping}{Default mapping (passed to \code{ggplot2::geom_line}).}

\item{data}{Default data (passed to \code{ggplot2::geom_line}).}

\item{formants}{Integer vector of formant numbers to keep.}

\item{...}{Additional arguments to \code{ggplot2::geom_line}.}
}
\value{
A \code{Layer} object.
}
\description{
Adds a \code{geom_line} layer over a \code{track_long} data frame, drawing one
line per (segment, formant) pair. Requires columns \code{rel_time},
\code{value}, \code{track}, plus a segment identifier built from
\code{session}/\code{bundle}/\code{start}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
long <- pivot_tracks_longer(quantify_result)
ggplot2::ggplot(long) + geom_formant_trajectory()
\dontshow{\}) # examplesIf}
}
