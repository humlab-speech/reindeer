% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_list_pivot.R
\name{pivot_tracks_wider}
\alias{pivot_tracks_wider}
\title{Pivot long-form track data back to wide form}
\usage{
pivot_tracks_wider(
  long,
  names_from = "track",
  values_from = "value",
  time_from = "rel_time"
)
}
\arguments{
\item{long}{A \code{track_long} tibble or compatible data frame.}

\item{names_from}{Name of the column holding the track family.}

\item{values_from}{Name of the column holding the measurement value.}

\item{time_from}{Name of the column holding the relative time.}
}
\value{
A tibble or \code{segment_list}.
}
\description{
Inverse of \code{\link[=pivot_tracks_longer]{pivot_tracks_longer()}}: takes a \code{track_long} tibble (or
anything with \code{track}, \code{rel_time}, \code{value} columns plus segment-identifying
columns) and reconstructs wide-form columns named \verb{<track>_<rel_time>}.
}
\details{
The result is a tibble, not a \code{segment_list} — pivoting back doesn't
recover the validator's required columns unless they were preserved on the
way in. When the input has all required segment columns plus a \code{db_uuid}
attribute, the result is upgraded to a \code{segment_list}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
long <- pivot_tracks_longer(wide)
restored <- pivot_tracks_wider(long)
\dontshow{\}) # examplesIf}
}
