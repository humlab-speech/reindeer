% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_list_pivot.R
\name{pivot_tracks_longer}
\alias{pivot_tracks_longer}
\title{Pivot DSP track measurements to long form}
\usage{
pivot_tracks_longer(
  seg,
  cols = NULL,
  names_to = "track",
  values_to = "value",
  time_to = "rel_time",
  .keep_metadata = TRUE
)
}
\arguments{
\item{seg}{A \code{segment_list} or \code{extended_segment_list}.}

\item{cols}{Tidyselect specification of which columns to pivot. Defaults
to \code{\link[=track_cols]{track_cols()}}.}

\item{names_to}{Name of the column receiving the track family (e.g.
\code{"F1"}).}

\item{values_to}{Name of the column receiving the measurement value.}

\item{time_to}{Name of the column receiving the relative time (in \verb{[0, 1]}
for wide-form columns; positional index normalised to \verb{[0, 1]} for
list-column tracks).}

\item{.keep_metadata}{If \code{TRUE} (default), metadata columns
(see \code{\link[=metadata_cols]{metadata_cols()}}) are carried through.}
}
\value{
A \code{track_long} tibble.
}
\description{
Convert wide-form track columns (e.g. \verb{F1_0.0, F1_0.1, ..., F1_1.0}
produced by \code{quantify(.at = seq(0, 1, 0.1))}) into long form with a
\code{track}, \code{rel_time}, and \code{value} column. List-column tracks
(per-segment numeric vectors) are unnested into one row per sample.
}
\details{
The long-form result is \strong{not} a \code{segment_list} (one segment maps to
many rows). It is a \code{track_long} tibble that carries \code{db_uuid},
\code{db_path}, and provenance as plain attributes for traceability.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs <- query(corp, "Phonetic == V")
wide <- quantify(segs, superassp::forest, .at = seq(0, 1, 0.1))
long <- pivot_tracks_longer(wide)
head(long)
\dontshow{\}) # examplesIf}
}
