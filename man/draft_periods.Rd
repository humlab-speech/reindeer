% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reindeer_annotate_python.R
\name{draft_periods}
\alias{draft_periods}
\title{Draft period annotations using Python/Parselmouth}
\usage{
draft_periods(
  corpus,
  bundles,
  level_name = "Periods",
  minimum_f0 = 75,
  maximum_f0 = 600,
  window_shape = "Gaussian1",
  interpolation = "cubic",
  relative_width = 1,
  parallel = FALSE,
  n_cores = parallel::detectCores() - 1
)
}
\arguments{
\item{corpus}{A corpus object}

\item{bundles}{A bundle_list or data.frame with session and bundle columns}

\item{level_name}{Name of the annotation level to create (default: "Periods")}

\item{minimum_f0}{Minimum F0 for period detection (Hz)}

\item{maximum_f0}{Maximum F0 for period detection (Hz)}

\item{window_shape}{Window shape for signal extraction}

\item{interpolation}{Intensity interpolation method}

\item{relative_width}{Relative width of extraction window}

\item{parallel}{Use parallel processing (default: FALSE)}

\item{n_cores}{Number of cores for parallel processing}
}
\value{
A \code{SuggestedEvents} object containing period annotations
}
\description{
Creates EVENT-level transcription suggestions for glottal periods detected
from acoustic signal. Uses Parselmouth (Python) for processing, replacing
external Praat calls.
}
\details{
This function returns a \code{SuggestedEvents} object that can be assessed,
corrected, and transcribed using the reindeer transcription system.
}
\examples{
\dontrun{
corp <- corpus("path/to/ae_emuDB")
bundles <- corp[".*", ".*"]  # All bundles

# Draft period annotations
suggestions <- draft_periods(corp, bundles)

# Assess and transcribe
assessment <- assess(suggestions)
print(assessment)

prepare(suggestions)
log <- transcribe(suggestions)
}
}
