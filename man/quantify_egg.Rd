% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/companion_eggstract.R
\name{quantify_egg}
\alias{quantify_egg}
\title{Quantify segments using an eggstract EGG processor}
\usage{
quantify_egg(
  seg,
  corpus = NULL,
  .using = NULL,
  ...,
  .at = NULL,
  .require_egg_flag = TRUE
)
}
\arguments{
\item{seg}{A \code{segment_list} or \code{extended_segment_list}.}

\item{corpus}{Optional \code{corpus}. Resolved from \code{seg@db_path} when NULL.}

\item{.using}{The eggstract DSP function to apply. Defaults to
\code{eggstract::ksvF0} when eggstract is installed. Pass any function
compatible with \code{\link[=quantify]{quantify()}}.}

\item{...}{Forwarded to \code{\link[=quantify]{quantify()}}.}

\item{.at}{Optional relative-time vector.}

\item{.require_egg_flag}{When \code{TRUE} (default), restrict to bundles
with bundle-level metadata \code{HasEGG = TRUE}. Set to \code{FALSE} to apply
\code{.using} to every row.}
}
\value{
An \code{extended_segment_list} with EGG-derived measurements.
}
\description{
Thin wrapper around \code{\link[=quantify]{quantify()}} that delegates DSP to a function from
the eggstract companion package (e.g. \code{eggstract::ksvF0}) and gates
availability via the bundle-level \code{HasEGG} metadata field. Use this
when the corpus mixes audio and EGG bundles and only the EGG-equipped
ones should be measured.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(eggstract)
segs <- query(corp, "Phonetic == V") |> biographize(corp)
quantify_egg(segs)
\dontshow{\}) # examplesIf}
}
