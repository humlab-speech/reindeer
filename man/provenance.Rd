% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_list_provenance.R
\name{provenance}
\alias{provenance}
\title{Inspect the provenance log of a segment_list}
\usage{
provenance(seg)
}
\arguments{
\item{seg}{A \code{segment_list} or \code{extended_segment_list}.}
}
\value{
A tibble with columns \code{step}, \code{verb}, \code{call}, \code{rows_in},
\code{rows_out}, \code{rows_lost}, \code{ts}. Empty if no provenance has been recorded.
}
\description{
Returns a tibble describing each pipeline step that touched the
\code{segment_list}: the verb name, the deparsed call, input/output row counts,
and the timestamp. Use this to surface silent row drops introduced by
navigation verbs (\code{scout}, \code{ascend_to}, \code{descend_to}) or dplyr filters.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs <- query(corp, "Phonetic == t")
segs <- scout(segs, 1)
provenance(segs)
\dontshow{\}) # examplesIf}
}
