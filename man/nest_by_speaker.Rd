% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_list_nest.R
\name{nest_by_speaker}
\alias{nest_by_speaker}
\title{Nest a segment_list by speaker}
\usage{
nest_by_speaker(seg, .key = "data", speaker_col = NULL)
}
\arguments{
\item{seg}{A \code{segment_list}.}

\item{.key}{Name for the list-column holding the per-group segment_lists.}

\item{speaker_col}{Name of the speaker column. When \code{NULL} (default),
the function auto-detects from a list of conventional names.}
}
\value{
A tibble with one row per speaker.
}
\description{
Returns a tibble with one row per speaker and a list-column of
per-speaker \code{segment_list} slices. The speaker column is detected
automatically (case-insensitive match on \code{Speaker}, \code{SpeakerID},
\code{speaker}, \code{speaker_id}) unless \code{speaker_col} is provided.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
segs |> biographize(corp) |> nest_by_speaker()
\dontshow{\}) # examplesIf}
}
