% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reindeer_enrich.R
\name{enrich.segment_list}
\alias{enrich.segment_list}
\title{Enrich segment_list method - join metadata or delegate DSP to quantify}
\arguments{
\item{object}{A segment_list (or extended_segment_list).}

\item{corpus_obj}{The corpus to pull metadata from (required when
\code{with = "metadata"}).}

\item{...}{Forwarded to \code{\link[=quantify]{quantify()}} when \code{.using} is supplied.}

\item{with}{One of \code{"metadata"} (default) or \code{NULL}. Set to \code{NULL} when
only running DSP via \code{.using}.}

\item{.using}{Optional DSP function; if supplied, delegates to \code{\link[=quantify]{quantify()}}.}
}
\value{
A segment_list (metadata-joined) or extended_segment_list (DSP).
}
\description{
\code{enrich(segs, corp, with = "metadata")} joins corpus metadata onto the
segment_list (folded in from the former \code{biographize()} entry point).
\code{enrich(segs, .using = fn)} delegates to \code{\link[=quantify]{quantify()}} for segment-level
DSP extraction so users only need to learn one verb.
}
