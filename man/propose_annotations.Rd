% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/companion_protoscribe.R
\name{propose_annotations}
\alias{propose_annotations}
\title{Propose draft annotations via the protoscribe companion package}
\usage{
propose_annotations(
  corpus,
  type = c("vad", "vot", "periods", "momel", "slam", "slam_plus", "slamp3"),
  .review = TRUE,
  .commit = FALSE,
  ...
)
}
\arguments{
\item{corpus}{A \code{corpus} object.}

\item{type}{One of \code{"vad"}, \code{"vot"}, \code{"periods"}, \code{"momel"}.}

\item{.review}{Logical. When \code{TRUE}, spawn \code{serve_app(corpus, seglist = suggestions)} so the user can accept/refine; when
\code{FALSE}, return the suggestions silently.}

\item{.commit}{Logical. Reserved for a future option to write back
accepted suggestions automatically. Default \code{FALSE} — review
only.}

\item{...}{Forwarded to the underlying protoscribe \verb{draft_*}
function.}
}
\value{
A \code{segment_list}-shaped tibble of suggestions, invisibly
when \code{.review = TRUE}.
}
\description{
Routes one of protoscribe's draft generators (VAD, VOT, periods,
MOMEL/INTSINT) over the corpus and either returns the proposed
annotations as a segment_list-shaped tibble for inspection or launches
the EMU-webApp via \code{\link[=serve_app]{serve_app()}} for interactive review.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(protoscribe)
propose_annotations(corp, type = "vad")
\dontshow{\}) # examplesIf}
}
